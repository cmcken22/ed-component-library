import { AvatarProps } from ".";
declare const Avatar: {
    ({ id, className, size, text, src, onHover, onClick, sx, tooltip, }: AvatarProps): import('../../node_modules/react/jsx-runtime').JSX.Element;
    defaultProps: Partial<AvatarProps>;
};
export default Avatar;
