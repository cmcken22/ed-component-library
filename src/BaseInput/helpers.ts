export const VariantMap: Record<string, string> = {
  table: "standard",
  standard: "standard",
  outlined: "outlined",
};

export const getFontColor = (color: string, value: any) => {
  if (!value && value !== 0) return "";
  return color;
  return "";
};
