import { GridItemProps } from '..';

declare const GridItem: {
    ({ id, className, xs, sm, md, lg, xl, sx, children, }: GridItemProps): import("react/jsx-runtime").JSX.Element;
    defaultProps: Partial<GridItemProps>;
};
export default GridItem;
