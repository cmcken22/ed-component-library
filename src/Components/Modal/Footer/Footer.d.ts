import { FooterProps } from './Footer.types';

declare const Footer: {
    ({ id, className, sx, children, onAccept, onCancel, acceptBtn, acceptBtnText, acceptBtnProps, cancelBtn, cancelBtnText, cancelBtnProps, }: FooterProps): import("react/jsx-runtime").JSX.Element;
    defaultProps: Partial<FooterProps>;
};
export default Footer;
