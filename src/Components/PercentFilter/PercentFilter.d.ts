import { PercentFilterProps } from './PercentFilter.types';

declare const PercentFilter: {
    ({ id, className, sx, label, placeholder, helperText, required, labelPosition, disabled, tooltip, variant, fullWidth, onChange, renderValue, }: PercentFilterProps): import("react/jsx-runtime").JSX.Element;
    defaultProps: Partial<PercentFilterProps>;
};
export default PercentFilter;
