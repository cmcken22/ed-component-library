import {
  ArgTypes,
  Canvas,
  Controls,
  Description,
  Markdown,
  Meta,
  Stories,
  Subtitle,
  Title,
} from "@storybook/blocks";
import * as SnackbarStories from "./Snackbar.stories";
import SnackbarProvider from "./SnackbarProvider";

export const markdownContent = `
\`\`\`jsx
<SnackbarProvider
  dense={true}
  maxSnack={5}
  autoHideDuration={3000}
  anchorOrigin={{
    vertical: "top",
    horizontal: "right",
  }}
>
  <App />
</SnackbarProvider>
\`\`\`
`;

<Meta of={SnackbarStories} />

<Title>Snackbar Provider</Title>

<Description of={SnackbarStories} />

<Markdown>{markdownContent}</Markdown>

<ArgTypes of={SnackbarProvider} />

<Title>Snackbar</Title>

<Canvas of={SnackbarStories.Default} />

<Controls of={SnackbarStories.Default} />

<Stories of={SnackbarStories} />
