import { RadioButtonProps } from "./";
declare const RadioButton: {
    ({ id, className, checked: passedValue, disabled, label, value, onChange, allowDeselect, labelPosition, sx, }: RadioButtonProps): import('../../node_modules/react/jsx-runtime').JSX.Element;
    defaultProps: Partial<RadioButtonProps>;
};
export default RadioButton;
