import { TabProps } from "./Tab.types";
declare const Tab: {
    ({ id, className, sx, label, value, children, disabled, }: TabProps): import('../../../node_modules/react/jsx-runtime').JSX.Element;
    defaultProps: Partial<TabProps>;
};
export default Tab;
